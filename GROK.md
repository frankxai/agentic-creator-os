# Agentic Creator OS — Grok Build Context

You have the Agentic Creator OS (ACOS) v12 available in this repository.

Read, in order:

1. `CLAUDE.md` — the canonical project instructions (design principles,
   CREATOR.md identity contract, hook doctrine, engineering rules)
2. `AGENTS.md` — work pattern and safety rules
3. `.grok/skills/` (if generated by `./install.sh --platform=grok`) — Grok-native
   skill seeds; plus `.claude/skills/` for the full catalog (Grok Build reads
   Claude-compatible skill layouts)

Skills auto-activate from context. Commands are markdown workflows under
`.claude/commands/` — execute them as described. Real asset counts live in
`STATS.md` (generated).

Adapter internals: `adapters/grok/` and the `grok-harness` skill.
