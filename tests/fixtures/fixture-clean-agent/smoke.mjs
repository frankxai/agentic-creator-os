// Existence-only stub. fixture-clean-agent is not a real, invokable agent —
// it exists solely so meta-agent-quality-auditor's check #9 (smoke fixture
// exists) can pass for the "known-good" fixture used in the auditor's own
// smoke eval (tests/fixtures/meta-agent-quality-auditor/smoke.mjs).
console.log('fixture-clean-agent smoke stub: OK (existence-only, no real behavior to test)')
