{
  "name": "@agentic-creator-os/email-mcp",
  "version": "1.0.0",
  "description": "Email and notification MCP server for Agentic Creator OS",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=build/index.js --format=esm --platform=node --target=node20 --packages=external",
    "typecheck": "tsc --noEmit",
    "dev": "tsc -w",
    "start": "node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "nodemailer": "^9.1.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.0",
    "@types/node": "^26.6.1",
    "esbuild": "^0.28.2",
    "typescript": "^5.0.0"
  }
}
